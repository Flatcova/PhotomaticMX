<% layout('./admin') -%>
<div class="row">
  <div class="col-lg-12 mt-5">
    <div class="card">
        <div class="card-body">
                <div class="media-body">
                    <h4 class="mb-3">Control de Información</h4>
                    Dentro de esta sección podras administrar las imagenes, junto
                    con sus descripcion de la pagina principal, como para anuncios o simmple
                    propaganda de la marca.
                </div>
                <a href="/admin/landing/newAdd">
                  <button type="button" class="btn btn-info mb-3"><i class="fas fa-plus-square"></i> Agregar Nuevo</button>
                </a>
        </div>
    </div>
  </div>
  <%for (var i = 0; i < data.length; i++) {%>
  <div class="col-lg-6 mt-5">
      <div class="card">
          <div class="card-body">
              <div class="media mb-5">
                  <img class="img-fluid mr-4" style="width: 30%;" src="<%= data[i].link %>" alt="image">
                  <div class="media-body">
                      <h4 class="mb-3"><%= data[i].titulo %></h4>
                      <%= data[i].descripcion %>
                  </div>
                  <ul>
                    <li>
                      <a href="/admin/landing/edit?id=<%= data[i]._id %>" class="btn btn-info mb-3"><i class="fas fa-edit"></i> Editar</a>
                    </li>
                    <li>
                      <a href="/admin/landing/delete?id=<%= data[i]._id %>" class="btn btn-danger mb-3"><i class="fas fa-trash"></i> Borrar</a>
                    </li>
                  </ul>
              </div>
          </div>
      </div>
    </div>
    <% } %>
</div>
